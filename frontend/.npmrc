# 内网构建机（Jenkins）连不上 registry.npmjs.org（read ETIMEDOUT），统一走阿里云
# npm 镜像。npm / pnpm / yarn 都读这个文件；全局安装（npm i -g pnpm）也吃它。
# 本地开发同样受益（国内拉包更快），要临时用官方源：npm --registry=https://registry.npmjs.org ...
registry=https://registry.npmmirror.com
